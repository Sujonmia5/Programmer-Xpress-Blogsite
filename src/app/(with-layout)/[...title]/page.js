import { usePathname } from 'next/navigation';
import React from 'react';

const DetailsProject = ({ perams }) => {
    // console.log(perams);
    return (
        <div>
            DetailsProject
        </div>
    );
};

export default DetailsProject;